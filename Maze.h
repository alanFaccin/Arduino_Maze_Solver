#ifndef MAZE_H
#define MAZE_H

#if defined(ARDUINO) && ARDUINO >= 100
#include "Arduino.h"
#else
#include "WProgram.h"
#endif

/*
#include "Intersection.h"
#include "Manoever.h"

class Maze
{
  public:
    Maze();
	
    Intersection _intersection;
    char* _path;   
    char* _shortestPath;    //Path _shortestPath;
    
  private:
	//Solution _solution;
	//Configuration
};
*/
#endif /* MAZE_H */
